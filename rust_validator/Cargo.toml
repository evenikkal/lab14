[package]
name = "rust_validator"
version = "0.1.0"
edition = "2021"

[lib]
name = "rust_validator"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module", "abi3-py39"] }
chrono = { version = "0.4", features = ["serde"] }

[build-dependencies]
